# Secure micro service

This is the "shade agent api" which runs in the TEE.

It can interact securely within its environment, and have exclusive conversation with the smart contract -- `is_verified_by_codehash`.
